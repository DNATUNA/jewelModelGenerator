version: "3.7"
services:
  jewelgenerator:
    container_name: jewelgenerator
    build: ./docker
    volumes:
      - ./app:/usr/src/app
    command:
      - /bin/bash
      - -c
      - "./docker/docker-entrypoint.sh"
